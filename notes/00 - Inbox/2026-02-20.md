# 2026-02-20
- monolith
    - "early-bound"
        - can be verified by the complier
- programming language choice for me is the one that allows me to express the "business rules" in such a way that invalid code is impossible
- we use TypeScript instead of JavaScript
- we really have to pay attention to coupling- especially in late bound calls
    - what if the api my angular app uses is released at a different cadence than my angular app and a change it has introduces something that will break my app?
    - what if we have a bunch of services using the same database and one of the apps' teams needs to remove or add a column in a table we are using?
    - how about we don't share dbs?
    - when you get really frustrated be "process" - when you are like "this is not what I wanted to do as a programmer!"
        - all of that is probably necessary. but it is also stuff we should (as devs and a company) be addressing